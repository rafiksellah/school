{% extends 'baseAdmin.html.twig' %}

{% block title %}School{% endblock %}

{% block body %}
    <h1>School</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ school.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ school.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td> {{school.address.street}}, {{school.address.city}}, {{school.address.postalCode}}  </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_school_index') }}">back to list</a>

    <a href="{{ path('app_admin_school_edit', {'id': school.id}) }}">edit</a>

    {{ include('admin/school/_delete_form.html.twig') }}
{% endblock %}
