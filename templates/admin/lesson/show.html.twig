{% extends 'baseAdmin.html.twig' %}

{% block title %}Lesson{% endblock %}

{% block body %}
    <h1>Lesson</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lesson.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ lesson.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_lesson_index') }}">back to list</a>

    <a href="{{ path('app_admin_lesson_edit', {'id': lesson.id}) }}">edit</a>

    {{ include('admin/lesson/_delete_form.html.twig') }}
{% endblock %}
